# ReviewMate 기술 의사결정 기록

이 문서는 ReviewMate의 기능 목록이 아니라, 개발하면서 내 판단이 어떻게 바뀌었는지를 기록한 문서다. 2026년 8월의 개발 노트와 실제 구현을 기준으로 **처음의 가정 → 발견한 문제 → 검토한 대안 → 선택 → 남은 한계** 순서로 정리했다.

> 결론부터 말하면, 이 프로젝트에서 가장 크게 배운 것은 “LLM을 연결하는 것”보다 **LLM이 틀릴 수 있다는 전제 아래 입력, 출력, 실행 경계와 사용자 경험을 설계하는 일**이 더 중요하다는 점이었다.

## 1. 왜 코드리뷰 서비스를 만들었는가

팀 프로젝트에서 같은 역할의 Controller, Service, Repository가 팀원마다 다른 구조로 작성됐다. 공통 메서드가 있는데도 비슷한 코드가 다시 만들어졌고, 예외 처리와 DTO 사용 방식도 일관되지 않았다. 프로젝트 초기에 정한 규칙은 개발이 진행될수록 흐려졌고, 사람 리뷰어가 같은 내용을 반복해서 확인해야 했다.

처음에는 “팀 규칙을 프롬프트에 넣고 변경 코드를 로컬 LLM에 보내면 1차 리뷰를 자동화할 수 있겠다”고 생각했다. 외부 AI를 바로 사용하지 않고 Ollama를 먼저 선택한 이유는 두 가지였다.

- Private Repository의 코드를 외부 서비스에 반복 전송하지 않는 실행 환경을 만들고 싶었다.
- 모델과 프롬프트를 여러 번 실험할 때 API 비용보다 로컬 자원을 먼저 활용하고 싶었다.

하지만 로컬 실행 자체가 보안을 완성해 주는 것은 아니다. GitHub Webhook을 받으려면 서버 엔드포인트는 외부에서 접근 가능해야 하고, GitHub API 사용을 위한 토큰도 필요하다. 따라서 내가 목표로 한 것은 “완전히 폐쇄된 시스템”이 아니라 **코드 본문을 처리하는 AI 추론 경로를 로컬에 둘 수 있는 선택지**였다.

또한 AI 리뷰는 사람의 승인이나 최종 판정을 대체하는 기능으로 정의하지 않았다. 처음부터 목표는 사람이 PR을 보기 전에 반복적인 위험과 팀 규칙을 확인하는 **1차 리뷰어**였다.

## 2. 리뷰 범위를 어떻게 줄였는가

### 처음 고려한 방식

초기 기획에서는 세 가지 입력 방식을 생각했다.

| 후보 | 기대한 장점 | 확인한 문제 |
| --- | --- | --- |
| 코드·diff 직접 입력 | 구현이 단순하고 빠르게 실험 가능 | 실제 협업 흐름과 분리됨 |
| 브랜치 간 diff | Git 작업 흐름과 연결 가능 | 사용자가 base/head를 직접 골라야 하고 PR 기능과 겹침 |
| 프로젝트 전체 분석 | 관련 코드까지 보며 정밀하게 판단 가능 | 로컬 모델의 context 한계와 처리시간 때문에 MVP에서 비현실적 |

처음에는 입력이 많을수록 리뷰 품질이 좋아질 것이라고 생각했다. 그런데 `qwen2.5-coder:7b`로 약 25,022자의 diff를 보냈을 때 정상적인 리뷰 대신 지시를 이해했다는 문장만 반환되는 현상을 확인했다. Java `StringBuilder`의 한계라고 의심했지만, `ollama ps`에서 실제 context가 4,096 tokens로 설정된 것을 확인했다. diff가 길어지면 앞부분의 팀 규칙과 출력 지시가 밀려날 수 있었다.

이 경험으로 “전체 저장소를 많이 넣는 것”과 “필요한 문맥을 정확히 주는 것”은 다르다고 판단했다. MVP는 다음 두 흐름으로 줄였다.

- **Quick Review:** 코드를 직접 입력해 빠르게 결과를 확인한다.
- **PR Review:** 열린 PR을 선택하고 GitHub가 제공하는 변경 파일과 patch를 입력으로 사용한다.

Branch Review를 없앤 이유는 실제 협업의 리뷰 단위가 PR이고, Branch와 PR의 차이가 결국 diff를 가져오는 방식에 집중되어 있었기 때문이다. 사용자 기능을 `QUICK + PR`로 줄이면 두 기능의 목적도 더 명확해진다.

### 현재 선택과 한계

현재 PR 리뷰는 변경 diff를 대상으로 하며 전체 저장소의 호출 관계를 분석하지 않는다. 그래서 한 파일만으로 판단할 수 없는 문제는 놓치거나 잘못 추론할 수 있다. 파일별 분할, 관련 파일 수집, hunk·method 경계 기반 chunking은 설계했지만 현재 저장소에서는 완성된 기능으로 주장하지 않는다.

## 3. 로컬 LLM을 어떻게 비교했는가

### 처음의 가정

처음에는 파라미터가 큰 coder 모델이 더 정확할 가능성이 높다고 생각했다. 그러나 코드리뷰 서비스에서는 정확도만큼 다음 조건도 중요했다.

- 실제 문제를 찾는가
- 정상 코드를 문제라고 지적하지 않는가
- 설명과 Evidence가 실제 코드와 일치하는가
- 같은 입력에서 판단이 얼마나 일관적인가
- 한국어와 JSON 출력 지시를 지키는가
- 긴 코드를 사용자가 기다릴 수 있는 시간 안에 처리하는가

그래서 인지도나 크기로 모델을 정하지 않고 같은 PC와 Ollama 환경에서 여섯 모델을 비교했다. 팀 규칙은 적용하지 않아 모델 자체의 Java/Spring 기본 리뷰 능력을 확인했다.

### 테스트 코드

- `UserService`: `Optional.get()`, `Long == Long`, 쓰기 메서드의 트랜잭션 누락, 메시지 없는 `IllegalArgumentException`
- `UserController`: Repository 직접 호출과 Entity 반환. 다만 팀 규칙이 없는 baseline이므로 계층 분리를 무조건 오답으로 채점하지 않았다.
- 정상 Transaction 코드: 클래스 수준 `@Transactional(readOnly = true)`와 쓰기 메서드 수준 `@Transactional` 조합
- 긴 `OrderService`: 2,827 input tokens에 여러 Optional, 객체 비교, 트랜잭션, 예외 처리, 재고 동시성 가능성 등을 분산

문제를 심어 둔 코드만 사용하면 많이 지적하는 모델이 좋아 보일 수 있다. 그래서 **정상 코드를 오탐하는지 확인하는 Q3를 별도로 둔 것**이 중요했다.

### 모델 6종 비교 결과

| 모델 | 생성 속도 | 관찰한 장점 | 결정에 영향을 준 문제 | 판단 |
| --- | ---: | --- | --- | --- |
| `qwen2.5-coder:14b` | 약 4.5~5.0 tok/s | Optional·Transaction 문제를 적극 탐지 | 정상 Transaction 패턴을 3회 모두 오탐, 일부 예외 설명 오류 | 품질 기준 모델 |
| `deepseek-coder-v2:16b` | 약 18.7~19.5 tok/s | 빠르고 일부 입력에서 반복 결과가 일정함 | 틀린 Evidence를 일관되게 제시하고 Transaction 판단이 실행마다 변함 | 빠른 대안 |
| `phi4:14b` | 약 4.5~4.8 tok/s | 일부 실행에서 예외를 정확히 설명 | 같은 코드의 예외 원인이 실행마다 달라지고 잘못된 수정 코드·영어 응답 발생 | 제외 |
| `devstral:24b` | 약 3.1 tok/s | 기본 문제 탐지 수는 비교적 양호 | `Optional.get()` 예외를 반복 오설명, 품질 상승폭에 비해 매우 느림 | 제외 |
| `qwen2.5-coder:32b` | 약 1.7~2.2 tok/s | 짧은 입력에서 설명이 비교적 깔끔함 | 긴 코드에서 8분 이상 걸리고도 오탐·미탐이 남음 | 서비스 후보 제외 |
| `qwen3-coder:30b` | 약 21.6~23.9 tok/s | 빠른 축에 속하고 긴 코드의 Evidence가 상대적으로 안정적 | 객체 비교, JPA·Transaction 설명에서 오탐·미탐 존재 | 최종 선택 |

`deepseek-coder-v2:16b`는 같은 오답을 반복하기도 했다. 이 결과로 **일관성과 정확도는 같은 지표가 아니라는 것**을 배웠다. 같은 답을 세 번 내도 세 번 모두 틀릴 수 있다.

### 긴 코드 직접 비교

| 항목 | `qwen2.5-coder:32b` | `qwen3-coder:30b` |
| --- | ---: | ---: |
| Input tokens | 2,827 | 2,827 |
| Output tokens | 838 | 750 |
| Generation speed | 1.7 tok/s | 21.8 tok/s |
| Generation time | 479.21초 | 34.47초 |
| Total time | 521.33초 | 68.79초 |
| 리뷰 품질 | 정답·부분정답·오탐 혼재 | 정답·부분정답·오탐 혼재 |

32B 모델은 긴 입력에서 약 14배 더 오래 걸렸지만 정확도가 그만큼 좋아지지 않았다. 그래서 `qwen3-coder:30b`를 선택했다. “가장 정확한 모델”이어서가 아니라 **내 실행 환경에서 감당할 수 있는 응답시간과 충분한 품질의 균형이 가장 좋았기 때문**이다.

### 수치를 해석할 때의 한계

이 표는 개발 중 남긴 개별 실행 기록이다. 출력 토큰 수도 다르고 전체 모델을 같은 횟수로 반복한 통계적 benchmark는 아니다. 당시 hardware 사양도 노트에 확정값으로 남아 있지 않아, 재현 문서로 만들려면 GPU·VRAM·RAM·Ollama 버전·quantization을 다시 기록해야 한다. 따라서 이 결과를 보편적인 모델 순위나 정확도 개선율로 표현하지 않는다.

## 4. 프롬프트를 길게 쓰면 오탐이 줄어들까

### 초기 접근: 모든 규칙을 한 번에 검사

처음에는 하나의 시스템 프롬프트에 일반 코드리뷰와 팀 규칙을 함께 넣었다. 모델에게 모든 규칙을 검사하도록 하면 누락이 줄어들 것이라고 생각했다. 실제로는 코드에 없는 setter나 Repository 로직을 추측하고, 근거가 부족해도 PASS 또는 VIOLATION 중 하나를 억지로 고르는 문제가 생겼다.

그래서 판정을 다음 네 상태로 나눴다.

- `VIOLATION`: 제공된 코드에서 위반을 직접 확인할 수 있음
- `PASS`: 적용 대상이 있고 준수 사실을 직접 확인할 수 있음
- `NOT_APPLICABLE`: 현재 코드에 규칙의 적용 대상이 없음
- `INSUFFICIENT_CONTEXT`: 관련성은 있지만 제공된 코드만으로 판단할 수 없음

근거가 없으면 판단을 보류하도록 만들자 환각은 줄었지만, 이번에는 중요한 규칙 일부만 골라 보고 나머지를 누락했다. “모든 규칙을 반드시 검토하라”는 지시를 추가하자 정상 코드에도 규칙을 억지로 끼워 맞추는 경향이 나타났다.

### 확인한 사실

2026년 8월 17일 실험에서는 다음을 바꿔 보았다.

- 긴 프롬프트와 단순한 프롬프트
- 한국어와 영어 지시
- 기술 스택 context 추가
- 팀 규칙 적용 여부
- 정상 코드와 문제 하나를 삽입한 mutation 코드

결론은 프롬프트를 길고 엄격하게 만드는 것만으로 품질 문제가 해결되지 않는다는 것이었다. 팀 규칙은 실제 위반을 찾는 데 도움을 주지만, 위반이 없을 때는 “규칙을 적용해야 한다”는 압력으로 오탐을 만들기도 했다.

9월 회고 노트에는 문맥이 거의 필요 없는 리뷰에서는 오탐이 약 10%, 보이지 않는 호출 흐름이 필요한 코드에서는 최대 60%까지 올라갔다고 적었다. 그러나 원시 테스트 케이스 수와 계산표가 함께 보존되어 있지 않으므로 이 수치를 확정 benchmark로 사용하지 않는다. 여기서 신뢰할 수 있는 결론은 **현재 diff 밖의 문맥이 필요한 판단에서 오탐 위험이 크게 증가했다**는 관찰이다.

### 선택: 한 기능, 두 개의 내부 리뷰

사용자에게는 하나의 Team Review를 제공하되 내부에서는 작업 목적을 분리했다.

```text
Team Review
├─ General Review → ReviewItem
└─ Rule Review    → RuleReviewItem
```

일반 리뷰는 문제를 자유롭게 찾고, 규칙 리뷰는 명시된 팀 규칙에 한정해 판정한다. 두 번 호출하므로 지연시간과 토큰 사용량이 증가하지만, 서로 다른 판단 기준을 한 응답에 섞지 않고 프롬프트·결과·통계를 따로 추적할 수 있다는 장점을 선택했다.

두 호출을 병렬화하지 않고 순차 실행한 현재 구조는 단순하고 실패 흐름을 관리하기 쉽지만, 지연시간 최적화 관점에서는 후속 비교가 필요하다.

## 5. 자유로운 자연어 응답을 왜 구조화했는가

### 실패한 접근

처음에는 프롬프트에 원하는 JSON 예시를 쓰고 Ollama의 `format: json`을 사용했다. 하지만 `format: json`은 JSON 문법만 보장할 뿐 `reviews` 배열 같은 애플리케이션 스키마를 보장하지 않았다. 실제로 다음처럼 파싱할 수는 있지만 서비스가 원하는 결과가 아닌 응답도 나왔다.

```json
{"response":"Understood. I will review the code."}
```

이 상태에서 `reviews`를 바로 순회하면 `NullPointerException`이 발생했다. 자연어로 출력 형식을 요구한 테스트에서도 실행마다 키와 중첩 구조가 달라졌다.

### 검토한 대안

| 대안 | 장점 | 문제 |
| --- | --- | --- |
| 원문 TEXT만 저장 | 구현이 가장 단순함 | 화면 표시·통계·GitHub 라인 연결이 어려움 |
| `JsonNode`로 유연하게 파싱 | 실험 단계에서 형식 변화 대응이 쉬움 | 필드 누락을 런타임에서 발견하고 방어 코드가 많아짐 |
| 응답 DTO + JSON Schema | 타입과 필수 필드를 명확히 강제 가능 | Schema와 DTO를 함께 유지해야 함 |

초기에는 구조를 자주 바꾸고 있었기 때문에 `JsonNode`로 실험했고, 응답 계약이 정리된 뒤 DTO와 Ollama JSON Schema 방식으로 옮겼다. JSON Schema는 시스템 프롬프트에 섞지 않고 Ollama 요청의 `format`에 전달했다.

또한 원본과 구조화 결과 중 하나만 고르지 않았다.

- `Review.rawResponse`: 당시 모델이 반환한 원문 보존, 파싱 오류 분석과 재현에 사용
- `ReviewItem` / `RuleReviewItem`: 검색, 화면, 통계, GitHub 리뷰 게시에 사용

모델에게 violation 개수까지 계산시키지 않고 서버가 배열 크기로 계산하도록 한 것도 같은 원칙이다. **확정적으로 계산할 수 있는 값은 LLM에 맡기지 않는다.**

## 6. 비동기로 바꾼 뒤 왜 저장한 리뷰를 찾지 못했는가

### 동기 처리의 문제

초기 Quick Review 요청은 한 HTTP 요청에서 Review 생성, GitHub/Ollama 호출, JSON 파싱, 결과 저장까지 모두 수행했다. 로컬 모델 응답이 길어질수록 사용자는 요청을 계속 기다려야 했고 `ReviewService`가 너무 많은 책임을 가졌다.

그래서 먼저 리뷰를 `PENDING`으로 저장하고 ID를 반환한 뒤 별도 스레드에서 처리하도록 변경했다.

### 첫 번째 실패: `save()`를 commit으로 생각했다

코드에서는 `save()` 다음 줄에서 비동기 메서드를 호출했기 때문에 저장이 끝났다고 생각했다. 그러나 비동기 스레드가 `findById(reviewId)`를 실행했을 때 “존재하지 않는 리뷰” 오류가 발생했다.

실제 순서는 다음과 같았다.

```text
트랜잭션 시작
→ Review와 Source save
→ 비동기 작업 예약
→ 비동기 스레드가 조회할 수 있음
→ 원래 메서드 종료
→ commit
```

ID가 객체에 할당된 것과 다른 트랜잭션에서 row가 보이는 것은 다른 문제였다. 여기서 `save() != commit()`이라는 차이를 실제 오류로 확인했다.

### 대안과 선택

| 대안 | 판단 |
| --- | --- |
| 비동기 작업에서 재시도·sleep | 타이밍 문제를 숨길 뿐 commit을 보장하지 않음 |
| 요청 전체를 동기로 유지 | 정합성은 단순하지만 긴 AI 요청을 사용자가 기다려야 함 |
| commit 이후 작업 시작 | 저장 완료를 실행 순서로 보장할 수 있음 |

현재는 `TransactionSynchronization.afterCommit()`에서 `ReviewAsyncService`를 호출한다. 리뷰 생성 트랜잭션이 실제로 commit된 뒤에만 별도 스레드가 조회를 시작한다.

### 두 번째 실패: 비동기 스레드의 LAZY 프록시

Review를 조회한 뒤 `review.getProject()`나 `systemSetting.getSystemPrompt()`에 접근할 때 `LazyInitializationException`도 발생했다. `@Async` 스레드는 원래 요청의 영속성 컨텍스트를 이어받지 않으며, Repository 호출이 끝난 뒤 LAZY 프록시가 실제 값을 읽으려고 해도 사용할 Session이 없었다.

검토한 해결책은 다음과 같았다.

| 대안 | 선택하지 않은 이유 |
| --- | --- |
| 모든 관계를 `EAGER`로 변경 | 필요한 시점과 무관하게 연관 데이터를 가져오고 조회 비용을 숨김 |
| Open Session in View에 의존 | HTTP 요청과 분리된 비동기 처리의 경계를 해결하지 못함 |
| 비동기 전체에 긴 `@Transactional` 적용 | GitHub·LLM 응답 대기 동안 DB 트랜잭션을 유지함 |
| 짧은 트랜잭션 안에서 DTO로 변환 | DB 작업과 외부 I/O 경계를 명확히 나눌 수 있음 |

최종 흐름은 다음과 같다.

```text
짧은 조회 트랜잭션
→ 필요한 Project·Prompt·Rule 값을 DTO로 복사
→ 트랜잭션 종료
→ GitHub API와 AI 호출
→ 짧은 저장 트랜잭션
→ ReviewItem·RuleReviewItem·Metrics·상태 저장
```

비동기화에서 가장 중요한 것은 스레드를 하나 더 만드는 일이 아니라 **commit 시점, 영속성 컨텍스트, 외부 I/O와 DB 트랜잭션의 경계를 정하는 일**이었다.

### 현재 한계

`@Async`는 프로세스 메모리 안의 작업이다. 애플리케이션이 재시작되면 실행 중 작업을 복원하지 못하고, 여러 인스턴스에서 PR별 순서를 보장하기도 어렵다. 현재 규모에는 단순한 구조를 선택했지만 재시도, 내구성, 순서 보장이 요구되면 메시지 큐나 작업 테이블 기반 worker와 비교해야 한다.

## 7. Ollama에 결합된 코드를 왜 분리했는가

처음에는 `OllamaClient`가 현재 시스템 프롬프트를 DB에서 조회하고 리뷰 유형까지 해석했다. 그러면 통신 클라이언트가 QUICK/PR, 일반/규칙 리뷰와 데이터 조회까지 알게 된다. OpenAI와 품질을 비교하거나 provider를 교체하려면 상위 로직까지 함께 수정해야 했다.

그래서 다음 경계를 만들었다.

```text
TeamReviewProcessor
→ AiReviewClient
   ├─ OllamaClient
   └─ OpenAiClient
```

Service가 사용할 프롬프트, user prompt, JSON Schema를 선택하고 Client는 HTTP 통신과 provider별 응답 변환만 담당한다. 입력·출력 token과 response time도 공통 Metrics로 축소했다.

이 선택은 “언젠가 provider가 늘어날 것”이라는 추상적인 확장성만을 위한 것이 아니었다. 로컬 모델의 오탐 한계를 확인한 뒤 OpenAI와 실제 결과를 비교하고, Private 코드·비용·품질 요구에 따라 실행 방식을 고를 필요가 생겼기 때문이다.

## 8. 결과를 생성한 뒤, 왜 GitHub 표시 방식을 다시 바꿨는가

초기에는 발견한 문제를 PR 리뷰 본문 하나에 모았다. 문제마다 코드, 설명, 대안, 수정 예시를 함께 넣자 4~5개만 발견되어도 같은 코드가 반복되고 사용자가 실제 문제 위치를 다시 찾아야 했다.

GitHub PR 화면에는 이미 diff가 보이므로 코드 본문을 댓글에 다시 복사할 필요가 없다고 판단했다. 그래서 역할을 분리했다.

- **인라인 댓글:** 실제 변경 라인의 문제와 권장 수정 방향
- **전체 요약:** 위험·주의·권고별 건수와 항목명

처음부터 multi-line review를 완성하기보다 `startLine`을 실제 diff의 추가 라인과 대조해 단일 라인에 연결하는 방식을 선택했다. 현재 구현은 시작 라인부터 최대 두 줄 뒤까지 유효한 변경 위치를 찾고, 연결할 수 없으면 요약에만 남긴다.

여기서 배운 것은 결과가 생성되는 것과 결과를 사람이 사용할 수 있는 것은 별개의 문제라는 점이다.

## 9. Webhook 자동화가 만든 새로운 문제

Webhook의 `opened`, `reopened`, `synchronize` 이벤트로 리뷰를 자동 시작하면 사용자가 별도로 요청하지 않아도 된다는 장점이 있다. 반면 PR에 push할 때마다 리뷰가 실행되므로 같은 문제를 다시 지적하고 요약 댓글이 계속 쌓일 수 있다.

현재 구현의 중복 검사는 다음 두 경우를 막는다.

- 같은 `X-GitHub-Delivery`가 재전송된 경우
- 같은 프로젝트·PR·head SHA의 리뷰 작업이 이미 접수된 경우

이것은 **같은 작업의 중복 실행**을 막는 장치이지, 서로 다른 commit에서 **같은 문제를 다시 게시하는 것**을 막는 장치는 아니다.

### 검토한 방향

| 문제 | 단순 대안 | 한계 | 다음 설계 방향 |
| --- | --- | --- | --- |
| push마다 요약표가 새로 생성됨 | 매번 새 댓글 작성 | PR Conversation이 계속 길어짐 | 완료 Delivery의 comment URL을 재사용해 최초 `POST`, 이후 `PATCH` |
| 같은 지적이 다시 게시됨 | 이전 AI 문장과 문자열 비교 | 모델 표현과 라인 번호가 바뀌면 동일 문제를 놓침 | 규칙 ID·파일·코드 문맥 기반 fingerprint로 추적 |
| 이번 결과에 없으면 해결로 볼 것인가 | 즉시 RESOLVED 처리 | 모델 미탐이나 분석 범위 제외일 수 있음 | 최신 코드에서 해결 근거를 확인하기 전까지 기존 상태 유지 또는 재확인 상태 |
| 이전 commit 리뷰가 늦게 끝남 | 완료 순서대로 게시 | 과거 결과가 최신 요약을 덮어쓸 수 있음 | PR별 게시 직렬화와 최신 Delivery 재검증 |

리뷰 실행 이력과 지속적으로 추적할 문제의 정체성을 분리하는 것이 핵심이다.

```text
ReviewRun       = 이번 head SHA에서 AI가 반환한 결과
TrackedIssue    = PR 안에서 동일 문제를 추적하는 상태와 fingerprint
SummaryComment  = PR별 하나만 유지하는 요약 댓글 ID
```

요약 댓글 갱신은 구현했다. DB에서 같은 프로젝트·PR의 기존 `commentUrl`을 찾고 GitHub HTML URL의 `issuecomment-{id}`를 사용해 최초에는 `POST`, 이후에는 `PATCH`한다. 기존 댓글이 직접 삭제되어 PATCH가 404를 반환하면 새 댓글을 생성해 복구한다. 같은 프로세스 안에서는 PR별 lock을 사용하고, 게시 직전에 더 큰 Delivery ID가 존재하는지 다시 확인해 오래된 리뷰가 최신 요약을 덮어쓰지 않게 했다.

다만 이 lock은 단일 애플리케이션 인스턴스에서만 유효하다. 여러 서버 인스턴스로 확장할 때는 DB advisory lock, 분산 lock 또는 PR별 메시지 큐 partition처럼 인스턴스 간 직렬화가 가능한 방식으로 바꿔야 한다. 또한 **같은 지적의 반복 인라인 댓글 방지**는 아직 구현하지 않았다. 이 문제에는 별도의 fingerprint와 해결 상태 정책이 필요하며, LLM 결과에 없다는 이유만으로 자동으로 “해결됨” 처리해서는 안 된다.

## 10. 지금 다시 만든다면

처음에는 좋은 모델과 긴 프롬프트가 있으면 품질이 해결될 것이라고 생각했다. 개발이 진행될수록 판단은 다음처럼 바뀌었다.

1. 모델 크기보다 정상 코드 오탐, Evidence 정확도, 응답시간을 함께 본다.
2. 입력을 무조건 늘리지 않고, 변경분과 판단에 필요한 문맥을 선택한다.
3. 일반 리뷰와 규칙 검사를 분리해 목적이 다른 작업을 한 프롬프트에 맡기지 않는다.
4. 구조화할 수 있는 계산과 검증은 서버에서 수행한다.
5. 비동기 처리에서는 Entity가 아니라 commit과 DTO 경계를 먼저 설계한다.
6. AI 결과는 최종 판정이 아니라 사람이 근거를 확인할 수 있는 보조 정보로 제공한다.
7. 자동화는 실행 횟수만 늘리는 것이 아니라 중복, 순서, 상태 조정까지 함께 설계한다.

현재 ReviewMate의 가치는 “AI가 정확한 리뷰를 해준다”는 주장에 있지 않다. 오탐과 지연, 불안정한 출력, 비동기 경계처럼 실제로 부딪힌 문제를 측정하고, 어디까지 시스템이 책임지고 어디서 사람의 판단을 남겨야 하는지 구체화한 과정에 있다.

## 근거 기록

- [기획 상세](https://app.notion.com/p/3ad5ae35342481019a6ac7eab8b47f98)
- [AI 프롬프트 활용 기록](https://app.notion.com/p/3b05ae3534248168b683e46ef08d9198)
- [코드리뷰 응답 구조화·비동기 처리](https://app.notion.com/p/3b85ae35342481a2b4c4e608812e6acc)
- [비동기 트랜잭션과 LLM Diff 분할](https://app.notion.com/p/3b95ae35342481d48005d8e652918418)
- [리뷰 타입·context·commit 시점 분석](https://app.notion.com/p/3ba5ae35342481039d97c084e616c1b6)
- [시스템 프롬프트·DTO·비동기 LAZY 문제](https://app.notion.com/p/3bd5ae35342481f7a9d9f48a4db173d0)
- [모델 6종 benchmark](https://app.notion.com/p/3bf5ae35342481aa8860cc71e09e2260)
- [프롬프트·팀 규칙 오탐 실험](https://app.notion.com/p/3bf5ae353424818f9cdbc5d0490c6c81)
- [Team Review 구조 분리](https://app.notion.com/p/3c55ae35342481388102e894e8e1870f)
- [AI Provider 추상화](https://app.notion.com/p/3c55ae353424819ca92cea26b1f511af)
- [Branch Review에서 PR Review로 전환](https://app.notion.com/p/3c55ae35342481849ff2f799c00ecee7)
- [QUICK 비동기화와 책임 분리](https://app.notion.com/p/3c55ae35342481f89a04fe1ca829b41d)
- [인라인 코드리뷰 전환 설계](https://app.notion.com/p/3cd5ae35342481aaa8bdd1469f012a91)

Notion 링크는 워크스페이스 접근 권한이 필요할 수 있다. 실험 수치는 당시 개발 기록을 옮긴 것이며, 이 문서를 작성하면서 새로 benchmark를 실행한 결과는 아니다.
